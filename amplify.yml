version: 1
frontend:
  phases:
    preBuild:
      commands:
        - curl -LsSf https://astral.sh/uv/install.sh | sh
        - uv sync
    build:
      commands:
        - mkdocs build
  artifacts:
    baseDirectory: site
    files:
      - "**/*"
  cache:
    paths: []
