version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pyenv install 3.13
        - pip install --upgrade pip
        - pip install -r requirements.txt
    build:
      commands:
        - mkdocs build
  artifacts:
    baseDirectory: site
    files:
      - "**/*"
  cache:
    paths: []
