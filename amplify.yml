version: 1
frontend:
  phases:
    preBuild:
      commands:
        - pyenv global 3.11
        - pip install -r requirements.txt
    build:
      commands:
        - mkdocs build
  artifacts:
    baseDirectory: site
    files:
      - "**/*"
  cache:
    paths: []
